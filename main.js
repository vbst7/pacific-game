import { createApp } from 'vue'
import App from './App.vue'

// If you recovered a router file, uncomment the lines below:
// import router from './router' 

const app = createApp(App)

// app.use(router)

app.mount('#app')
